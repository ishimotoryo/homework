<p id="notice"><%= notice %></p>

<h1>蔵書一覧</h1>
<div class="panel panel-default">
<div class="panel-heading">好みの本を探してみよう</div>
<div class="panel-body">
  <p>蔵書のあらゆる情報をみることができます</p>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>画像</th>
      <th>タイトル</th>
      <th>作者</th>
      <th>値段</th>
      <th>出版社</th>
      <th>出版日</th>
      <th>コード番号</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td>
        <img src="<%= book.image %>" class="img-responsive " >
        </td>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= number_to_currency(book.price , unit: '¥' , precision: 0) %></td>
        <td><%= book.publisher %></td>
        <td><%= book.sold %></td>
        <td><%= book.code %></td>
        <td><%= link_to '閲覧', book  , class: "btn btn-info btn-xs"%></td>
        <td><%= link_to '編集', edit_book_path(book) , class: "btn btn-success btn-xs" %></td>
        <td><%= link_to '削除', book, method: :delete, data: { confirm: '本当に消しますか？' } , class: "btn btn-danger btn-xs" %></td>
      </tr>
    <% end -%>
  </tbody>
</table>
</div>
<br>

<%= link_to '新しく登録', new_book_path , class: "btn btn-primary btn-lg"%>
